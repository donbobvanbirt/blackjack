import React from 'react';
import { render } from 'react-dom';

import Layout from './components/Layout';   // component that describes the layout

render(
  <Layout />,
  document.getElementById('root')
)
